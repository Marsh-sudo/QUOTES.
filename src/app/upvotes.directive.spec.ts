import { UpvotesDirective } from './upvotes.directive';

describe('UpvotesDirective', () => {
  it('should create an instance', () => {
    const directive = new UpvotesDirective();
    expect(directive).toBeTruthy();
  });
});
